[project]
name = "ode-neural-surrogate"
version = "0.1.0"
description = "Neural network surrogate for ODE-based biochemical simulations with mass-balance and toxicity analysis."
requires-python = ">=3.9"
